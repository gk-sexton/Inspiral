import { connect } from 'react-redux';
import Collection from './collection';

const mapStateToProps = ({ session, collections }) => {
  return  {
  };
};

const mapDispatchToProps = (dispatch, someprops) => {
  debugger
  return {
  };
};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(Collection);
